<%= form_for(glibrary) do |f| %>
  <% if glibrary.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(glibrary.errors.count, "error") %> prohibited this glibrary from being saved:</h2>

      <ul>
      <% glibrary.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
